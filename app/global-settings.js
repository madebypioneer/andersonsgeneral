export const baseUrl = `https://inside2.andersonsgeneral.com`;
export const apiUrl = `https://inside2.andersonsgeneral.com/wp-json`;
export const revalidateInterval = 60;

/*
 * Shopify collections included in a production build.
 * Collection pages and product pages both use this list.
 */
export const builtCollectionHandles = Object.freeze([
  'hats-headwear',
  'misc',
  'tops',
]);
